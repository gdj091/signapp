<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.signapp.mapper.DocumentMapper">
    <insert id="insertDocument">
	    INSERT INTO document (title, content, uploader_no)
	    VALUES (#{title}, #{content}, #{uploaderNo})
	</insert>
	<select id="selectAllDocuments" resultType="com.example.signapp.dto.Document">
	    SELECT docId, title, content, uploader_no, createdAt
	    FROM document
	    ORDER BY createdAt DESC
	</select>
</mapper>